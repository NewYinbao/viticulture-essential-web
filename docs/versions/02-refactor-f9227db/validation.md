# 验证记录：工程重构与 EE 规则收口

- 提交：`f9227db2ed09b8c37df9fe4eabb08ee497589a30`
- 主要证据：[testing.md](records/testing.md)、[rules-review.md](records/rules-review.md)、[ui-review.md](records/ui-review.md)。

## 当时记录为通过的检查

| 检查 | 当时记录 |
| --- | --- |
| Go 默认测试 | 全部通过 |
| `ee_rule_audit` | 通过 |
| `audit_diff` | 通过；公共 Plant 拔藤差异已关闭 |
| `go vet ./...` | 通过 |
| usability 浏览器测试 | 7 组通过 |
| rules-fix 浏览器测试 | 5 组通过 |
| EE 访客初始选项夹具 | 141 个场景通过 |
| 自然完整对局 | 1 局，11 年后按年末终局完成 |
| 弃牌流程 | 建房、父母选择、四季流程、桌面和手机视口通过 |
| Windows 原生 EXE 冒烟 | health、网页、鉴权、建房/加入/开始、落盘和重启恢复通过 |

自然局同时记录了 HTTP/SSE 手牌隔离、旧 revision 拒绝、轮次推进、断线追帧和服务重启恢复。浏览器测试使用随机本地端口和临时数据目录。

## 覆盖边界

- 141 个访客初始选项是夹具场景，不是 141 局自然对局，也不代表全部嵌套组合穷举。
- 390×844 是浏览器视口检查，不是实体手机测试。
- 本地 WSL 没有 C 编译器，因此当时未运行 `go test -race`；仓库只是新增了 CI race 配置，不能由“配置存在”推断远端已执行。
- Planner/Organizer 组合、20 分后回落以及年末展示顺序仍留作解释边界。

本页不引用后续提示、棋盘内卡片面板、新手引导或扩展版本的测试结果。
