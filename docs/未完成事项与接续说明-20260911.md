# 葡萄酒庄园：未完成事项与接续说明

> 后续已按用户要求恢复开发。本文件保留停止时的历史快照；本轮新增密码、规则修复与验证进展请看 [玩家密码与续作记录](玩家密码与续作记录-20260911.md)。

> **状态：已按用户要求停止开发，整体仍为 partial，不是完整扩展交付。**
>
> 停止时间：2026-09-11 22:28:46（UTC+08:00）。停止位置：Moor 访客规则资料核对阶段。
> 本文依据停止后的源码、阶段结果、审查记录及既有执行日志整理；停止后没有继续实现、编译、运行测试或启动新审查。

## 1. 停止与保留情况

- 本次开发控制器、主 Codex 及归属子进程已停止；停止操作核对的 17 个进程身份均无存活残留。
- 每 5 分钟执行的完成监测任务已删除，不会自动继续开发、复审或发送该任务的完成通知。
- 原有两份 `Viticulture.exe` 服务以及原 Codex 桌面服务仍在运行，未被停止。
- 未读取或修改真实对局存档，未切换现用 3013 服务，未覆盖现用 `dist/Viticulture.exe`。
- 已写入的源码、测试、文档、构建和证据全部保留；没有回滚、清理未提交文件、提交或推送 Git。
- 只有用户另行明确要求继续时，才应恢复开发。不要直接重新运行旧控制器。

### 工程与记录位置

- 当前源码：`C:\Users\admin\Games\Viticulture\essential-web`
- 对应 WSL 路径：`/mnt/c/Users/admin/Games/Viticulture/essential-web`
- 本轮任务记录目录：`artifacts/expansions/full-completion-20260911-172053/`
- 停止核验：上述目录的 `stop-result.json`
- 停止时源码指纹：上述目录的 `source-at-stop.json`
- 停止时 Git 工作区状态：上述目录的 `git-status-at-stop.txt`
- 本轮开始前源码备份：上述目录的 `source-before.zip`、`source-before.json`

工作区有大量已修改及 untracked 文件，它们包含实际实现，不能只读取 Git HEAD 或仅查看已跟踪文件的 diff。`artifacts/` 中的重要交接证据也需要单独保留。

## 2. 已确认范围与当前状态

本任务限定竞争模式的 **Tuscany Essential、Moor Visitors、Visit from the Rhine Valley**。不包括 World 合作模式、Bordeaux、旧 Tuscany 额外模块或促销内容。

| 模块 | 停止时已有内容 | 仍未完成的部分 |
| --- | --- | --- |
| EE 本体 | 原有本体保留；既有回归继续执行 | 新扩展全部接入后的最终本体回归与旧存档兼容复验 |
| Tuscany 主板与影响力 | 四季行动、个人过季、起床、影响力、主行动 UI、代表性恢复路径已实现并可配置 | 全部扩展组合、多层待决、跨季多人及自然终局验收 |
| 11 类特殊工人 | 目录、两类公开牌池、培训加价、身份/次年可用、能力、中文操作与定向测试已落地；阶段报告为 ready/clean | 与所有建筑、新访客及预约/年末流程的完整组合验证；模块审查不等于整包审查 |
| 36 张建筑牌 | 目录、有限牌堆、EE 四轮开局轮抽、Tuscany side 2 获取、建造/拆除/占田、私人行动、持续/年末效果及代表浏览器流程已落地 | 逐张自然浏览器触发、全部分支与嵌套恢复、合法组合自然终局；最新建筑审查仍为 issues |
| Moor 40 张访客 | 官网与资料线索已查找；尚处资料核对阶段 | 完整真实牌效、牌库、中文 UI、规则引导与测试均未完成 |
| Rhine 80 张访客 | 已有官网基础资料和替换牌组规则说明 | 完整真实牌效、独立替换牌库、四张 Tuscany 专用牌的具体依赖过滤、中文 UI 与测试均未完成 |
| 整体交付 | 有独立扩展开发版 EXE 与启动器 | 最终全量验收、整包独立新会话 review、修复后复审及最终发布报告 |

### 当前开局保护的真实状态

`internal/game/expansions.go` 的 `ExpansionConfig.Playable()` 仍拒绝 `visitors != "ee"`。

因此，当前代码允许 EE/Tuscany 主板及建筑、特殊工人组合进入开发版开局，但 **EE+Moor 与 Rhine 仍不能作为可用牌组开局**。不能把前端存在选项、目录数量或模块说明等同于功能完成。

停止时源码指纹与 `009-moor` 会话开始时相同，且该会话没有文件变更事件。这次 Moor 会话没有新增生产代码或测试实现，不能把资料搜索描述成已完成牌效。

## 3. 尚未实现：Moor 40 张访客

以下均为待完成项目：

- [ ] **逐牌规则证据**：确认全部 40 张的身份、夏/冬归属、费用、金币/得分/收入区别、数量、可选/必选、目标和触发时机。
- [ ] 以出版社卡面、规则与适用勘误为权威；公开玩家整理表仅作交叉核对。无法确认的金额、图标、分支或裁定逐条标记，不得猜测。
- [ ] **真实效果实现**：为每张牌接入实际执行分支与可行性校验，不用统一奖励、本体效果或通用演示逻辑代替。
- [ ] **EE+Moor 有限牌库**：初始化、抽牌、弃牌、洗回、牌身份及存档恢复；与 Rhine 保持隔离。
- [ ] **多人及连续效果**：正确的响应者、参与资格、费用结算、外层续步、预约/跨季恢复，防止重复收益。
- [ ] **隐藏信息顺序**：抽牌颜色和数量在看见牌面前按规则确定；不能允许窥牌后取消或重选。
- [ ] **中文界面**：逐牌效果、分支、所需资源、可用/不可用原因、实际操作控件及私有手牌选择。
- [ ] **中文规则与新手引导**：按本局配置及当前选择定位，不套一个泛化提示；断线/切房/多人响应时不泄露私有信息。
- [ ] **测试**：逐牌正常路径、费用上下界、非法选择、错误响应者、重复提交、JSON/HTTP 恢复、UI 操作及跨模块交互。

### 已取得的线索与未解决证据问题

- 出版社页面：<https://stonemaiergames.com/games/viticulture/moor-visitors-expansion/>
- 出版社整套照片：<https://stonemaiergames.com/wp-content/uploads/2016/02/all-cards.jpg>
- BGG 文件页面元数据已返回成功：<https://boardgamegeek.com/filepage/148716>；对应文件 ID 为 `190685`，文件名为 `Viticulture Moor Visitor Card List.pdf`。
- **元数据成功不等于已取得、读完或核实该 PDF**。停止时没有形成完整的、可验收的 40 张权威效果表。
- 已有照片存在分辨率、遮挡或图标辨认问题，不能据此假定所有牌效清晰可读。
- 官网 HTML/正文在 `artifacts/expansions/evidence/moor-official.html`、`moor-official.txt`。
- 本轮图片临时文件仍在：
  - `C:\Users\admin\AppData\Local\Temp\viticulture-moor-all-cards.jpg`
  - `C:\Users\admin\AppData\Local\Temp\moor-mpc.jpg`
- TEMP 文件可能被系统清理；如未来继续，应先核对这些证据是否仍可用。参考图不代表有权把商业美术放进产品。

## 4. 尚未实现：Rhine 80 张访客及依赖过滤

- [ ] 完成 80 张（夏季 40、冬季 40）的逐牌规则证据、真实效果、全部分支与中文说明。
- [ ] 实现 **替换访客牌库**，不是追加混洗：不得同时混入 EE、Moor 或其他不兼容访客。
- [ ] 确认 **四张 Tuscany 专用牌的确切身份及逐张模块依赖**，不能只根据总开关或牌名推断。
- [ ] 针对 EE/Tuscany 主板、独立建筑、独立特殊工人及其合法组合过滤牌库；不能所有组合无条件固定投入 80 张。
- [ ] 处理同名或相同效果重印牌的身份，避免把旧牌重复混入替换牌组。
- [ ] 接入有限抽弃牌、回收、存档版本、错误回滚、隐藏选择、多人响应与跨季续步。
- [ ] 接入全部真实 UI 控件、中文牌效、可用性提示、规则面板与新手引导。
- [ ] 完成逐牌效果、模块依赖、牌库互斥、公私 View、重启恢复、自然对局及跨模块测试。

出处入口：<https://stonemaiergames.com/games/viticulture/visit-from-the-rhine-valley/>

现有资料在 `artifacts/expansions/evidence/rhine-official.html`、`rhine-official.txt`、`rhine-comments-page1.html` 等。现有资料与官网示例不构成完整逐牌签核。

## 5. 已有实现但未充分验收：Tuscany

### 5.1 36 张建筑逐牌验收

已有实现不应推倒重做。主要入口为：

- `internal/game/structures.go`
- `internal/game/structure_draft.go`
- `internal/game/structures_test.go`
- `internal/game/placement.go`、`choices.go`、`resources.go`、`seasons.go`、`tuscany_seasons.go`
- `web/static/js/action-panel.js`、`ee-ui.js`、`building-catalog.js`、`rules-content.js`

仍需：

- [ ] 在实际浏览器及真实服务中逐张触发全部 36 张，而不是仅以目录检查、直接函数调用或少数代表流程代替。
- [ ] 对每张牌建立效果分支、费用、目标资格、占田/拆除、持续/私人行动/年末触发及相互作用的覆盖记录。
- [ ] 验证每个多步效果在待决阶段断线、保存、重启后的恢复，并返回正确外层动作。
- [ ] 验证建筑与所有特殊工人、EE/Moor/Rhine 访客及不同主板组合的相互作用。
- [ ] 对逐牌来源进行精确复核：规则书的“建筑通则”页不自动证明每一张卡的费用、图标和效果；每张卡需要可定位的卡面/文字/勘误证据。
- [ ] 补齐当前浏览器脚本未覆盖的真实资源选择和 UI 分支；禁止用伪造 View 隐藏服务端字段遗漏。

已修复并有阶段回归的例子包括 School 培训与特殊工人选择、Charmat 配方边界、Tavern 资源精确选择、Statue 终局阈值、Harvest Machine 全田收获、Mercado 年末待决恢复。这些是保留成果，**不是剩余未修复 bug 列表**，后续组合验证仍应保护它们。

### 5.2 11 类特殊工人与主板整体验收

主要入口：`special_workers.go`、`special_worker_actions.go`、`special_workers_test.go`、`tuscany*.go`、`visitor_special.go`、`visitor_sequence.go`，均在 `internal/game/`。

- [ ] 把现有工人能力测试扩展到建筑、Moor、Rhine 全部接入后的组合与自然对局。
- [ ] 验证 Planner/Messenger 预约、Organizer/Manager、多层访客及建筑选择之间的外层续步恢复。
- [ ] 验证个人过季、年末处理、起床、收入、回收/陈酿/弃牌、额外收益与最终年判定的完整顺序。
- [ ] 验证中途拒绝/放弃奖励、重复请求及重启不会丢失动作、消耗两次工人或重复结算。
- [ ] 在所有相关人数与组合下复验占位、奖励资格、工人费用、特殊身份和培训上限。

特殊工人阶段的 ready/clean 是**组件范围报告**，不能替代上述整包组合验收或最终独立 review。

## 6. 最新审查仍未关闭的事项

来源：`artifacts/expansions/structures-independent-review-20260911-r4.json`，状态为 **issues**。

| 编号 | 级别 | 未解决事项 |
| --- | --- | --- |
| STRUCTURES-ACCEPTANCE-001 | P2 | 全部 24 种合法配置尚未由真实玩家 View 策略完成合法开局到自然终局 |
| STRUCTURES-ACCEPTANCE-002 | P2 | 36 张建筑尚未逐张在自然浏览器流程中触发并覆盖每张牌的嵌套恢复分支 |
| STRUCTURES-SCOPE-003 | P1 | 整体仍缺 Moor、Rhine 与四张 Tuscany 依赖访客，不能生成完整扩展通过证据 |

这些包含验收缺口和未实现范围，不应全部描述成已经复现的运行时崩溃；但也不能因代表测试通过就把它们关闭。

## 7. 全部组合、隐私、规则引导与自然终局验收

### 7.1 配置矩阵

约定的结构组合为：

```text
board: ee | tuscany
structures: false | true
specialWorkers: false | true
visitors: ee | ee_moor | rhine
```

共 24 种结构组合。Rhine 的合法牌库必须再按已核实的具体依赖过滤；“组合结构合法”不等于“当前已实现可开局”。

- [ ] 全部合法组合真实建房、房主配置、加入、开局；非法配置、非房主、旧 revision、开局后修改拒绝且状态不变。
- [ ] 每个组合由只使用本人 View 和公开规则的策略，从真实开局持续到自然终局。
- [ ] 不修改分数、资源、隐藏牌序或存档来制造终局；明确区分 UI 点击、授权 API 策略和测试夹具。
- [ ] 记录首次达到结束阈值、继续完成当年、最终计分与胜者判定。
- [ ] 对自然对局中未出现的卡/分支单独补测试，不把“某个全局结束了”当全部牌效覆盖。

### 7.2 持久化、事务与隐私

- [ ] View/HTTP/SSE/轮询只公开应公开的信息；对手手牌、牌堆顺序、私有选择不得泄露。
- [ ] 选择 ID、revision、响应者、费用和目标资格严格验证；无效动作及存档失败不改变权威状态。
- [ ] 实际待决时停止自己的测试 EXE，以同一临时存档重启，使用原 token 恢复后继续完成动作/对局。
- [ ] 断线期间其他玩家真实行动，重连必须用新的状态/SSE 证据追上，而不是让旧缓存冒充恢复成功。
- [ ] 旧存档没有扩展字段时仍按 EE 默认处理；新扩展状态序列化与恢复完整。

### 7.3 中文界面、规则与新手引导

需求全文：`docs/expansions-help-requirements.md`，属于必做交付条件，不是可选美化。

- [ ] Moor/Rhine 所有牌面、分支、费用、数量、条件和不可操作原因准确对应服务端。
- [ ] 建筑/特殊工人及跨模块待决全部具有真实控件、正确目标定位和可理解的中文提示。
- [ ] 只提示当前响应者，其他玩家看到等待；原手牌区点选、上抬高亮、再次取消，避免新增独立弃牌页。
- [ ] 引导可开关、跳过、重看；切房/断线/重连无旧配置、焦点或高亮残留。
- [ ] 使用实际桌面与手机 viewport 截图查看遮挡、溢出、破图、字体和焦点。手机 viewport 不是实体手机测试。
- [ ] 说明与资源随 Go 内嵌，日常游玩不增加在线依赖；商业原图未获明确授权时不直接复用。

## 8. 最终测试、独立 review 与发布仍未执行

停止时已核实：

- 控制器的全量独立测试记录 `gate_runs` 为空。
- 最终独立审查通过记录 `clean_reviews` 为空。
- `tests/expansions-full-browser-test.cjs` 尚不存在。
- `artifacts/expansions/full-acceptance.json` 尚不存在。
- `artifacts/expansions/coverage-full.json` 尚不存在。

因此后续仍需：

- [ ] 完成独立的全量浏览器/自然对局验收脚本，而不是把当前代表流程脚本改个名字。
- [ ] 形成全量卡牌覆盖表，分别记录目录、真实效果、来源、分支与测试证据；不能仅验数量。
- [ ] 在最终源码上重新执行 Go 普通/审计回归、vet、适用 JS 测试及原生 Windows 浏览器验收。
- [ ] 实现完成后**新开不继承实现会话上下文的独立 review session**，审查实际源码、untracked 文件、规则证据、测试质量与所有遗留问题。
- [ ] 发现问题后修复、重新跑测试，再新开会话复审；本次原验收约定为两次不同会话无未解决问题，且受审源码与测试/构建产物一致。
- [ ] 复核实际 Windows EXE 哈希和内嵌资源，不能拿修改前的二进制或旧截图验收新源码。
- [ ] 更新 README、发布说明、进度、机器结果与前端可用性提示，清理互相矛盾的旧状态描述。
- [ ] 只有全量条件满足才标记 complete/passed；审查没发现问题不代表软件绝对零错误。

### 已有测试的正确解释

已查阅的阶段日志记录了 Go 普通回归、审计标签、vet，以及扩展/帮助/Tuscany UI 浏览器代表测试成功。双 Edge 曾真实执行轮抽、建造、配置同步、私有 View 和重启恢复。

这些是停止前的阶段结果，**不是本次停止交接时重新运行的测试，更不是全量扩展通过结果**。既有报告中的 `fixtureOnly=false` 也不能推出全部牌和组合都自然覆盖了。

## 9. 保留的开发版及接续注意

### 当前开发版

- EXE：`dist/Viticulture-Expansions.exe`
- 启动器：`Start-Viticulture-Expansions.cmd`
- 启动器配置：监听 `0.0.0.0:3014`，存档 `runtime/expansions-data`
- 停止时实测 EXE SHA-256：

```text
5724522ab1bd5ac6f227d1f155cd80ed90b8e202048210ec0c843b838015228d
```

该哈希与最新建筑阶段报告一致。本次没有运行启动器，也没有将该开发版切换到现用服务；文件存在不代表完整扩展已发布。

### 若用户以后另行要求继续

1. 先读本文、`docs/expansions-implementation-task.md`、`docs/expansions-help-requirements.md` 及阶段证据，再核对实时源码和 Git 状态。
2. 保留已写建筑与特殊工人实现，从 Moor 的规则证据/逐牌实现、Rhine 及明确验收缺口继续，不重新推倒前面阶段。
3. **不要直接重新执行旧 `controller.py`**：它会重新建立任务并可能从最早阶段开始，不是安全的断点恢复入口。
4. 原控制器把“本阶段剩余”和“全项目剩余”共用一个 `remaining` 列表，曾导致模块已报 ready 仍重复执行。接续任务应明确区分 `phase_remaining` 和 `project_remaining`，仅在阶段证据满足时推进；最终总验收仍要求整个项目无未完成项。
5. 不创建并行写同一工作区的多个实现者；审查应独立且避免修改被审源码。优先保存每个连贯代码批次及其测试结果。
6. 保持现用服务、真实存档、原 EXE 和 Cloudflare 功能不动。测试只用自己的随机回环端口、TEMP 存档和自有进程。
7. Windows 默认 Go 不在 PATH；既有工具链位于 WSL `/opt/viticulture-toolchain/go/bin/go`。原生 Node/Edge 已用于阶段验证。PowerShell 读中文要显式 UTF-8，带逗号的 Go 标签参数要加引号。此处仅为接续说明，本次没有执行这些开发步骤。

### 关键证据索引

- `artifacts/expansions/implementation-result.json`
- `artifacts/expansions/structures-acceptance.json`
- `artifacts/expansions/structures-coverage.json`
- `artifacts/expansions/special-workers-coverage.json`
- `artifacts/expansions/structures-independent-review-20260911-r4.json`
- `artifacts/expansions/full-completion-20260911-172053/004-special-workers.json`
- `artifacts/expansions/full-completion-20260911-172053/008-structures.json`
- `artifacts/expansions/full-completion-20260911-172053/009-moor.jsonl`（中途停止的资料核对日志，不是完成报告）
- `docs/expansions-progress.md`、`docs/expansions-release.md`（含历史状态，需结合源码和报告判断）

---

**交接结论：开发已停止；已有成果保留。Moor、Rhine、全量组合/逐牌自然流程验收，以及最终整包独立 review 和发布仍未完成。**
